---
# Backup docker volumes
- name: Create backup of docker volumes
  ansible.posix.archive:
    path: "{{ docker_volumes_path }}/*"
    dest: "{{ nas.backups.mount_point }}/docker/docker-volumes-{{ ansible_date_time.iso8601 }}.tar.gz"
    format: gz
  become: true
  register: backup_archive_result
